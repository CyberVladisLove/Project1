{% extends 'base.html.twig' %}

{% block title %}Guest{% endblock %}

{% block body %}
    <h1>Гость</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ guest.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ guest.name }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ guest.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_guest_index') }}">назад к списку</a>

    <a href="{{ path('app_guest_edit', {'id': guest.id}) }}">изменить</a>

    {{ include('guest/_delete_form.html.twig') }}
{% endblock %}
